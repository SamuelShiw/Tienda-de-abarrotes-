<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_screen"
    android:orientation="vertical"
    android:padding="@dimen/space_16">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Productos con stock bajo"
        android:textColor="@color/ios_text_primary"
        android:textSize="@dimen/text_xl"
        android:textStyle="bold" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_8"
        android:text="Detecta faltantes en tienda y repón rápidamente"
        android:textColor="@color/ios_text_secondary"
        android:textSize="@dimen/text_sm" />

    <TextView
        android:id="@+id/tvEmptyProductosBajos"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_24"
        android:gravity="center"
        android:text="No hay productos con stock bajo"
        android:textColor="@color/ios_text_secondary"
        android:textSize="@dimen/text_md"
        android:visibility="gone" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvProductosBajos"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="@dimen/space_16"
        android:layout_weight="1" />

</LinearLayout>